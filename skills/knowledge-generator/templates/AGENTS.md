<!--
  AGENTS.md 通用协作约束文件模板 | 模板版本: 2.0.0
  适用场景: 项目中不存在 .trae/rules/ 或其他协作约束文件时，由 knowledge-generator skill 自动在项目根目录创建。
  使用方式: 复制到项目根目录，替换 {项目名称} 等占位符，按实际项目补充章节内容。
  注意: 占位符必须替换，禁止原样保留。
-->

# {项目名称} 协作约束

本文件定义 AI 与本项目协作时的规约，应始终注入 AI 会话上下文。

## 项目概述

{项目是什么、解决什么问题、技术栈}

## 项目结构

| 模块 | 职责 | 源码目录 |
| --- | --- | --- |
| {模块名} | {职责描述} | {源码目录路径} |

## 编码规范

{命名规范、注释规范、异常处理规范等}

## 知识库

知识库入口：`docs/knowledge/知识库索引.md`

### 知识库维护规则

> 当任务涉及代码修改，且修改内容覆盖知识库已记录的模块、类、业务流程时，必须主动检查并同步更新对应知识库文档。

维护流程：
1. 代码修改完成后，检查本次修改涉及的类/模块
2. 在知识库索引中搜索是否有对应的知识库文件
3. 若有命中，读取对应知识库文件，更新受影响的内容
4. 更新完成后执行质量校验（路径存在性、关键词重复、核心链路回溯）

## 问题定位

{按问题类型定位到模块和文件的规则}
