<!-- 模板版本: 2.5.0 | 适配语言: Vue / React 前端项目 -->
<!-- 空值处理规则: 核心文件表中缺项时整行删除，不留空行；表头固定不可改；其余章节缺内容时写"暂无"而非删除章节 -->

# {领域名称}

## 是什么

{业务对象的简述，回答这个知识库覆盖什么问题。说明前端涉及的业务场景，如用户交互流程、页面状态管理、API 数据获取与展示等}

## 核心文件

| 类型 | 文件 |
| --- | --- |
| 路由/页面入口 | {路由定义文件或页面组件路径} |
| 页面组件 | {主页面组件路径} |
| 状态管理 | {Store/Hook/Context 路径} |
| API 服务 | {API 请求模块路径} |
| 类型定义 | {Interface/Type 定义路径} |
| 公共组件 | {复用组件路径} |

## {核心链路/核心逻辑标题}

{按子场景拆解，每个子场景包含：}
- 入口（路由跳转 / 用户交互事件 / API 调用触发）
- 关键逻辑（组件状态流转、API 调用链、数据转换、副作用处理）
- 注意点（边界条件、兼容逻辑、性能优化、错误处理）

## 状态流转

{用文字或 ASCII 图描述前端状态管理的状态机。每个状态标注对应的状态变量或枚举值。}

```
IDLE ──> LOADING ──> SUCCESS
                    │
                    └──> ERROR ──> IDLE（重试）
```

状态流转规则：
- {列出合法的前后状态对应关系，禁止跳跃的状态}
- {状态流转的触发条件（用户操作 / API 响应 / 定时器等）}

## 排查建议

{问题定位优先顺序}

## 高风险点

{每个风险点必须包含：类型标签、具体位置、影响场景、建议处理方式}

- **[类型标签]** {风险描述}
  - 类型：`[可靠性]` / `[性能]` / `[代码缺陷]` / `[安全]` / `[兼容性]`
  - 位置：{文件路径 + 行号或函数名}
  - 影响场景：{什么情况下会触发}
  - 建议处理：{修复方向或规避方案}

## 架构上下文

> 本领域中涉及的横切架构特征，跨领域共享的架构决策。无相关内容时写"暂无"。

### 状态管理架构

{该领域使用的状态管理方案（Pinia/Vuex/Redux/Zustand/Context），简要说明。如：订单状态使用 Pinia Store 集中管理，通过 computed 派生派生订单列表}

### 组件架构

{组件层级结构、组件通信方式（props/emit/provide-inject/事件总线）。如：订单列表页采用 Container-Presenter 模式，OrderListContainer 负责数据获取，OrderListPresenter 只负责渲染}

### 数据架构

{前端缓存策略、数据持久化、本地存储等。如：购物车数据持久化到 localStorage，通过 watch 监听自动同步}

### 路由架构

{路由结构、路由守卫、懒加载策略。如：订单相关页面采用懒加载，通过路由前置守卫校验登录状态}

## 待补充（可选）

> **本章节为可选**：仅在 .summary.json 的 coverageGaps 非空时保留。
> 当 coverageGaps 为空（领域已充分覆盖）时，删除整个章节，不保留空壳。
> 内容从 coverageGaps 同步生成，不独立维护，保持单一数据源。

### 核心子场景
> 对应 coverageGaps 中 gapType=core 的条目。不允许超过 2 条，超过则领域不允许标记为「已完成」。

- {核心子场景描述}

### 实现细节
> 对应 coverageGaps 中 gapType=detail 的条目。无上限。

- {实现细节描述}
