<!-- 模板版本: 2.3.0 | 适配语言: Java/JVM 系（非 JVM 项目见底部多语言映射说明） -->
<!-- 空值处理规则: 核心文件表中缺项时整行删除，不留空行；表头固定不可改；其余章节缺内容时写"暂无"而非删除章节 -->

# {领域名称}

## 是什么

{业务对象的简述，回答这个知识库覆盖什么问题}

## 核心文件

| 类型 | 文件 |
| --- | --- |
| Web 入口 | {Controller/Handler 路径} |
| 服务接口 | {Service 接口路径} |
| 服务实现 | {ServiceImpl 路径} |
| 业务编排 | {Manager 路径} |
| 数据对象 | {DO/PO/DTO 路径} |
| 枚举 | {Enum 路径} |

## {核心链路/核心逻辑标题}

{按子场景拆解，每个子场景包含：}
- 入口（Controller 路由/Facade 方法/Job 触发）
- 关键逻辑（核心方法调用链、状态流转、金额计算）
- 注意点（边界条件、兼容逻辑、高风险操作）

## 状态流转

{用文字或 ASCII 图描述核心状态机。每个状态标注对应的枚举值。}

```
INIT ──> CHARGED ──> SETTLED ──> PAID
                                    │
                                    ├──> REFUNDING ──> REFUNDED
                                    └──> REVERSING ──> REVERSED
```

状态流转规则：
- {列出合法的前后状态对应关系，禁止跳跃的状态}
- {状态流转的触发条件和操作类型}

## 排查建议

{问题定位优先顺序}

## 高风险点

{每个风险点必须包含：类型标签、具体位置、影响场景、建议处理方式}

- **[类型标签]** {风险描述}
  - 类型：`[可靠性]` / `[并发]` / `[代码缺陷]` / `[安全]` / `[性能]`
  - 位置：{文件路径 + 行号}
  - 影响场景：{什么情况下会触发}
  - 建议处理：{修复方向或规避方案}

## 架构上下文

> 本领域中涉及的横切架构特征，跨领域共享的架构决策。无相关内容时写"暂无"。
> 详细架构说明参见对应的架构专题文档（如有）。

### 部署架构

{该领域在云端/岛端/多机房部署中的差异，如果有。如：本领域在云端直调 Agent，岛端通过 MQ 透传}

### 设计模式

{该领域使用的核心设计模式（策略/责任链/观察者等），简要说明。如：开票流程采用策略模式，按发票类型路由到不同 Processor}

### 数据架构

{分库分表/双写双读/数据隔离等，如果有。如：商品明细表采用分片双写，通过 ShardingDao 实现旧表到分片表的过渡}

### 外部依赖架构

{MQ/缓存/配置中心等基础设施的使用方式。如：使用 Redis 分布式锁防止并发开票，key 格式为 invoice:lock:{invoiceId}}

## 待补充

> 待补充条目必须分为两类，核心子场景不允许超过 2 条，超过则领域不允许标记为「已完成」。

### 核心子场景
> 该领域核心业务流程的子场景，有独立的调用链和状态流转。不允许超过 2 条。

- {核心子场景描述}

### 实现细节
> 配置、外部依赖、边缘逻辑等辅助信息。无上限。

- {实现细节描述}

---

<!--
多语言映射说明（非 JVM 项目使用本模板时替换核心文件表的类型名）：

| Java 概念 | Python 对应 | Go 对应 | Node.js 对应 |
| --- | --- | --- | --- |
| Controller/Handler | View/Route | Handler | Controller/Route |
| Service 接口 | Service 抽象基类 | Service interface | Service interface |
| ServiceImpl | Service 实现类 | Service impl | Service impl |
| Manager | Service（编排层） | UseCase/Service | Manager/Orchestrator |
| DO/PO/DTO | Model/Schema | Struct/DTO | Model/DTO |
| Enum | Enum/常量类 | const/iota | Enum/常量对象 |

映射后保持表格结构不变，只替换"类型"列的名称。
-->
