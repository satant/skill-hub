<!-- 模板版本: 2.5.0 | 适配语言: Vue / React 前端项目 -->
<!-- 空值处理规则: 各表格缺项时整行删除，不留空行；表头固定不可改；其余章节缺内容时写"暂无"而非删除章节 -->

# {数据模型主题}

## 是什么

{前端数据模型覆盖的 Interface/Type 定义，回答这些类型定义覆盖什么业务数据}

## 核心类型定义

| 类型名 | 文件 | 用途 |
| --- | --- | --- |
| {TypeName} | {文件路径} | {用途简述} |

## 字段字典

{按类型分组，列出关键字段的业务含义、取值约束、与其他类型的关联}

### {TypeName}

| 字段名 | TS 类型 | 业务含义 | 取值约束 / 枚举 | 关联类型 |
| --- | --- | --- | --- | --- |
| {fieldName} | {string/number/boolean/...} | {含义} | {约束或枚举值} | {关联类型名} |

## 枚举值

{状态枚举、类型枚举的完整取值清单}

### {枚举常量名}

| 枚举值 | 含义 |
| --- | --- |
| {VALUE} | {业务含义} |

## API 数据映射

{前端 Interface 与后端 API 响应的映射关系，包括字段名转换、数据格式化等}

### {API 接口名}

| 前端字段 | API 响应字段 | 转换逻辑 |
| --- | --- | --- |
| {前端字段名} | {API 字段名} | {直接映射 / 类型转换 / 格式化说明} |

## 关系图

{类型与类型之间的引用关系，用文字或 ASCII 图描述}

```
{TypeA}.{字段} ──> {TypeB}
```

## 排查建议

{数据异常问题定位优先顺序}

## 待补充

{后续应继续沉淀的内容}
