<!-- 模板版本: 2.1.0 | 适配语言: Java/JVM 系（非 JVM 项目见底部多语言映射说明） -->
<!-- 空值处理规则: 各表格缺项时整行删除，不留空行；表头固定不可改；其余章节缺内容时写"暂无"而非删除章节 -->

# {数据模型主题}

## 是什么

{模型覆盖的业务对象与表，回答这个知识库覆盖什么数据}

## 核心表

| 表名 | 对应 DO | 职责 |
| --- | --- | --- |
| {table_name} | {XxxDO 路径} | {表职责简述} |

## 字段字典

{按表分组，列出关键字段的业务含义、取值约束、与其他表的关联}

### {表名}

| 字段名 | 类型 | 业务含义 | 取值约束 / 枚举 | 关联表.字段 |
| --- | --- | --- | --- | --- |
| {field_name} | {varchar/bigint/...} | {含义} | {约束或枚举值} | {外键或逻辑关联} |

## 枚举值

{状态枚举、类型枚举的完整取值清单}

### {枚举类名}

| 枚举值 | 编码 | 含义 |
| --- | --- | --- |
| {ENUM_VALUE} | {code} | {业务含义} |

## 关系图

{表与表的外键/逻辑关联，用文字或 ASCII 图描述}

```
{表A}.{字段} ──> {表B}.{字段}
```

## 排查建议

{数据异常问题定位优先顺序}

## 待补充

{后续应继续沉淀的内容}

---

<!--
多语言映射说明（非 JVM 项目使用本模板时替换 DO 概念）：

| Java 概念 | Python 对应 | Go 对应 | Node.js 对应 |
| --- | --- | --- | --- |
| DO/PO | Model/ORM类 | Struct/Model | Model/Schema |
| Enum | Enum/常量类 | const/iota | Enum/常量对象 |
| Mapper/DAO | Repository | Repository | Repository/DAO |

映射后保持表格结构不变，只替换对应列的名称。
-->
