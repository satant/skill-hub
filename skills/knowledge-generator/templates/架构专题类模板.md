<!-- 模板版本: 2.3.0 | 适配语言: 语言无关 -->
<!-- 适用场景: 横切架构特征（部署模式/设计模式/数据架构/基础设施）的系统性描述 -->
<!-- 空值处理规则: 各章节缺内容时写"暂无"而非删除章节 -->

# {架构主题名称}

## 是什么

{本架构专题解决什么问题，覆盖哪些横切架构特征，回答"为什么需要这个架构"}

## 架构决策记录

> 记录关键架构决策的"为什么"，而非"是什么"。每个决策必须包含：背景、决策、理由、后果。

### 决策1：{决策标题}

- **背景**：{决策时的上下文和面临的问题}
- **决策**：{选择了什么方案}
- **理由**：{为什么选择这个方案而非其他}
- **后果**：{这个决策带来的影响，包括正面和负面}

## 架构拓扑

{用文字或 ASCII 图描述架构的整体结构}

```
{架构拓扑图示例}
云端请求 ──> Web Controller ──> CloudProcessor
                              │
岛端请求 ──> Web-Proxy Controller ──> IslandProcessor ──> MQ
```

## 影响范围

{该架构特征影响哪些业务领域，列出受影响的领域文档清单}

| 业务领域 | 受影响的方式 | 详见 |
| --- | --- | --- |
| {领域名称} | {如何受此架构影响} | {领域文档路径} |

## 核心机制

{该架构的核心工作机制，按子机制拆解}

### {子机制1标题}

- **触发条件**：{什么时候触发}
- **执行流程**：{核心步骤}
- **关键代码**：{实现该机制的类和方法}

| 机制 | 核心类 | 文件路径 |
| --- | --- | --- |
| {机制描述} | {类名} | {文件路径} |

## 数据架构

{如果涉及数据层面的架构（分库分表/双写双读/数据隔离等），描述数据流转机制}

- **存储策略**：{分库分表规则/数据隔离字段}
- **同步机制**：{Canal/MQ/定时任务等数据同步方式}
- **一致性保障**：{最终一致性/强一致性如何保证}

## 部署差异

{如果涉及多部署模式（如云端/岛端/多机房），描述不同部署的差异}

| 维度 | 云端 | 岛端 |
| --- | --- | --- |
| 入口 | {云端入口} | {岛端入口} |
| 数据 | {云端数据特征} | {岛端数据特征} |
| 依赖 | {云端外部依赖} | {岛端外部依赖} |

## 排查建议

{架构层面问题的定位优先顺序}

## 高风险点

{架构层面的风险点}

- **[类型标签]** {风险描述}
  - 类型：`[可靠性]` / `[并发]` / `[代码缺陷]` / `[安全]` / `[性能]`
  - 位置：{文件路径 + 行号}
  - 影响场景：{什么情况下会触发}
  - 建议处理：{修复方向或规避方案}

## 待补充（可选）

> **本章节为可选**：仅在 .summary.json 的 coverageGaps 非空时保留。
> 当 coverageGaps 为空时删除整个章节，不保留空壳。
> 内容从 coverageGaps 同步生成，保持单一数据源。

### 核心子场景
> 对应 coverageGaps 中 gapType=core 的条目。不允许超过 2 条。

### 实现细节
> 对应 coverageGaps 中 gapType=detail 的条目。
